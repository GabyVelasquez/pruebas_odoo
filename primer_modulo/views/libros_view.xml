<?xml version="1.0" ?>
<odoo>
    <!--VISTA FORMULARIO LIBROS -->
    <record id="libros_form_view" model="ir.ui.view">
        <field name="name">libro.form.view</field>
        <field name="model">libros</field>
        <field name="arch" type="xml">
            <form string="LibroF">
                <header>
                    <button name="boton_publicar" string="Publicar" type="object" states="draft" class="oe_highlight"/><!--states: visible cuando este en "draft"-->
                    <button name="boton_borrador" string="Borrador" type="object" states="published"/>
                    <field name="state" widget="statusbar"/><!--options="{'clickable':1}"-->
                </header>
                <sheet>
                    <group><!--Bootstrap 12 columnas-->
                        <group col="1">
                            <field name="image" widget="image" class="oe_left oe avatar" nolabel="1"/>
                        </group>
                        <group col="2"/>
                        <group col="2" colspan="2"><!--6 columnas-->
                            <field name="name"/>
                            <field name="editorial"/>
                            <field name="isbn"/>
                            <field name="autor_id"/>
                            <field name="lastname_autor"/>
                            <field name="categoria_id"/>
                            <field name="description"/>
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!--VISTA ÁRBOL LIBROS -->
    <record id="libros_tree_view" model="ir.ui.view">
        <field name="name">libro.tree.view</field>
        <field name="model">libros</field>
        <field name="arch" type="xml">
            <!--
            decoration-muted: light grey
            decoration-danger: light red
            decoration-success: light green
            decoration-primary: light purple
            decoration-info: light blue
            decoration-warning: light brown
            decoration-bf: bold
            decoration-it: italic
            -->
            <tree string="Libros" decoration-success="state=='published'" decoration-info="state=='draft'">
                <field name="name"/>
                <field name="editorial" optional="show"/>
                <field name="isbn" optional="show"/>
                <field name="autor_id" optional="show"/>
                <field name="categoria_id" optional="show"/>
                <field name="state" widget="badge" decoration-info="state=='draft'" decoration-success="state=='published'"/>
            </tree>
        </field>
    </record>

    <!--VISTA KANBAN-->
    <record id="libros_kanban_view" model="ir.ui.view">
        <field name="name">libro.kanban.view</field>
        <field name="model">libros</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="id"/>
                <field name="name"/>
                <field name="editorial"/>
                <field name="isbn"/>
                <field name="autor_id"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <div class="o_kanban_image">
                                <img t-att-src="kanban_image('libros','image',record.id.raw_value)" alt="Image"/>
                            </div>
                            <div class="oe_kanban_details">
                                <div class="o_kanban_record_top">
                                    <div class="o_kanban_record_heading">
                                        <strong class="o_kanban_record_title oe_partner_heading">
                                            <field name="name"/>
                                        </strong>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!--VISTA BÚSQUEDA-->
    <record id="libros_search_view" model="ir.ui.view">
        <field name="name">libros.search.view</field>
        <field name="model">libros</field>
        <field name="arch" type="xml">
                <search>
                    <!--Búsqueda rápida-->
                    <field name="name" string="Nombre del lbro"/>
                    <field name="autor_id" string="Nombre del autor"/>
                    <!--Filtro rápido-->
                    <filter name="categoria_cientifico" string="CientíficoZ" domain="[('categoria_id','=','Científico')]"/>
                    <!--Agrupación rápida-->
                    <group expand="0" string="Agrupar por" colspan="12" col="10">
                        <filter name="categoria" string="CategoríaW" context="{'group_by':'categoria_id'}"/>
                        <filter name="autor" string="AutorW" context="{'group_by':'autor_id'}"/>
                    </group>
                    <!--Panel de filtros-->
                    <searchpanel>
                        <field name="categoria_id" string="CategoríasQ" icon="fa-users"/>
                        <field name="autor_id" string="AutoresQ"/><!--si no se coloca icon, entonces aparece ícono carpeta-->
                    </searchpanel>
                </search>
        </field>
    </record>

</odoo>